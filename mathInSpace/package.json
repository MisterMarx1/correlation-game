{
  "name": "mathinspace",
  "version": "1.0.0",
  "main": "constants.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:www",
    "build:www": "node scripts/build-www.js",
    "cap:sync": "npm run build && npx cap sync android && node scripts/apply-admob-config.js",
    "cap:open": "npx cap open android"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/safe-area": "^6.0.0"
  }
}
